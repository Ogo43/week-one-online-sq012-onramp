function sumMix(arr) {
    let sum = 0;
    // declare your loop
   for(let i=0; i<arr.length; i++) {
     console.log(arr[i])
     sum = sum + Number(arr[i]);
   }
     // add the numbers and the string on the array
     console.log(sum)
       } 
  module.exports = sumMix